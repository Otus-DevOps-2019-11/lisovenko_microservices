version: '3.3'
services:
  post_db:
    image: mongo:3.2
    volumes:
      - post_db:/data/db
    networks:
      back_net:
        aliases:
          - comment_db
  ui:
    image: ${USERNAME}/ui:${VER}
    ports:
      - ${EXT_P}:${INT_P}/tcp
    networks:
      - front_net
      - back_net
  post:
    image: ${USERNAME}/post:${VER}
    networks:
      - back_net
      - front_net
  comment:
    image: ${USERNAME}/comment:${VER}
    networks:
      - back_net
      - front_net
volumes:
  post_db:

networks:
  front_net:
    external: true
  back_net:
    external: true
